@type mapkeys.v1.Flags

# An identifier key on a bool K matches nothing: an identifier names a
# field, and a map has none. "True" is not a bool literal in any case.
# MUST be rejected.

by_flag = { True: "v" }
